Tetra Regression Testing
========================

This is a lightweight python project designed as a framework to run regression tests,
compare results and store results in a database.


Quick Start
===========

`$ cd tetra
 $ python3 setup.py develop --user
`
